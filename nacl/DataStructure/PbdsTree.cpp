#include <bits/extc++.h>
using namespace __gnu_pbds;
/*
tree<int,null_type,less<int>,rb_tree_tag,tree_order_statistics_node_update> tr;
//same as rope<int>, except tr.lower_bound(x) and upper_bound
tr.find_by_order(k); //return kth iterator; k=[0,tr.size()), out of this will
get tr.end() tr.order_of_key(val); //return rank(val); tr.join(tr2); //merge tr
and tr2, tr2.clear() tr.split(const int&r,RBTree&tr2); //<r will in tr, >=r will
in tr2
*/